@if (Request.IsAuthenticated)
{
    <li><a href="#">Hello, @User.Identity.Name</a></li>
    <li><a href="@Url.Action("LogOff", "Account")">Log off</a></li>
}
else
{
    @*<li>@Html.ActionLink("Log in", "ExternalLogin", "Account", new { provider = "microsoft" }, null)</li>*@
}
