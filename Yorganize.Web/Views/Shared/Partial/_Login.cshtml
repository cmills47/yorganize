<div class="form">
    @if (Request.IsAuthenticated)
    {
        <a href="#">Hello, @User.Identity.Name</a> 
        <span>|</span>
        <a href="@Url.Action("LogOff", "Account")">Log off</a>
       
        <a href="#" id="cog-menu"><span><i class="icon-cog"></i></span></a>
       
    }
    else
    {  
        @Html.ActionLink("Log in", "ExternalLogin", "Account", new { provider = "microsoft" }, null)
    }
</div>
